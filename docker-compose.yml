version: "3.9"
services:
  chatapp:
    image: <your-dockerhub-username>/nodejs-chat-app:${TAG:-latest}
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
